# Application name
spring.application.name=demo

# Server port
server.port=9136

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/LoanEligibilityEmiRiskChecker?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ❌ REMOVE THIS LINE (deprecated & unnecessary)
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=mySecretKeyForJWT12345678901234567890
jwt.validity=86400000
mvn 